# Canopy

A desktop widget framework built on top of Arbor.

## Status

Early scaffold. The core library currently re-exports Arbor and defines the Canopy namespace.

## Build

```bash
cd canopy
lake build
```

## Dependencies

- Arbor (widget tree + layout + render commands)
